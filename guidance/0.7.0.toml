# Transcribed from this release's own changelog entry, which is the only
# briefing it shipped with.

schema = "sdd.guidance/1"
release = "0.7.0"

[[steps]]
id = "re-point-the-retired-rule"
kind = "rule-retired"
breaking = true
destinations = ["specs"]
actor = "operator"
text = "re-point-the-retired-rule.md"

[[steps]]
id = "declare-the-fixture-paths"
kind = "gate-widened"
breaking = true
destinations = ["declaration"]
actor = "operator"
text = "declare-the-fixture-paths.md"
