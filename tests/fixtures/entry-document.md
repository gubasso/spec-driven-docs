# Bound the lifetime of an access token

An entry document from the plan zone, included so the typed-clause gate has something to check and so
the spec-to-code seam has a worked instance. Its heading shape belongs to the planning tool, not to
this framework; only the `Amends` clauses below are this framework's business.

## Amends

- `_docs/specs/SPEC-auth.md` — ADDED `auth:token-expiry-is-bounded`
- `_docs/specs/SPEC-auth.md` — MODIFIED `auth:refresh-requires-reauth`

## Acceptance

- The rule `auth:token-expiry-is-bounded` has a `Verify:` command, and that command exits zero.
